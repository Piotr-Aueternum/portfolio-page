// breakpoints
xs = 480px
sm = 768px
md = 992px
lg = 1200px
lap = 1400px
lg = 1870px

min(arg)
	@media (min-width arg)
		{block}
max(arg)
	@media (max-width arg - 1)
		{block}
btw(argo, argt)
	@media (min-width argo) and (max-width (argt - 1))
		{block}